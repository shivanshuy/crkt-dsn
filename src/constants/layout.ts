/** Grid and component footprint (aligned with canvas CSS grid). */
export const GRID_SIZE = 24

/** Multiples of GRID_SIZE so tops/edges sit on grid lines */
export const COMPONENT_WIDTH = 96
export const COMPONENT_HEIGHT = 48

/** Minimum canvas size (px); scroll container allows building larger circuits than the viewport */
export const CANVAS_MIN_HEIGHT = 4000
export const CANVAS_MIN_WIDTH = 4000

/** Sticky note — min / default outer size (px) */
export const TEXT_NOTE_MIN_WIDTH_PX = 72
export const TEXT_NOTE_MIN_HEIGHT_PX = 40
export const TEXT_NOTE_DEFAULT_WIDTH_PX = 140
export const TEXT_NOTE_DEFAULT_HEIGHT_PX = 56

/** Note body typography (px) */
export const TEXT_NOTE_FONT_SIZE_MIN_PX = 8
export const TEXT_NOTE_FONT_SIZE_MAX_PX = 28
export const TEXT_NOTE_FONT_SIZE_DEFAULT_PX = 12
/** Header bar label (when header is visible) */
export const TEXT_NOTE_HEADER_FONT_DEFAULT_PX = 10
