/** Default download base name when the export field is empty. */
export const EXPORT_DEFAULT_BASENAME = 'crkt-export'

export const EXPORT_FILENAME_MAX_LENGTH = 120

/** Extra empty space below the diagram when exporting full height (scroll needed). */
export const EXPORT_EXTRA_BOTTOM_PX = 48
